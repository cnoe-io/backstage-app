/**
 * Alpha entry point for the terraform backend plugin.
 * Re-exports the plugin for use with the new Backstage backend system.
 *
 * @alpha
 */
export { terraformPlugin as default } from './plugin';
