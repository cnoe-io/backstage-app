apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backstage-kro-patch
  namespace: argocd
  labels:
    env: dev
spec:
  project: default
  source:
    repoURL: cnoe://backstage-patch/manifests
    targetRevision: HEAD
    path: "."
  destination:
    server: "https://kubernetes.default.svc"
    namespace: backstage
  syncPolicy:
    automated:
      selfHeal: true
